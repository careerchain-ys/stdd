# PLAN: Phase 1-A — `packages/core/` 基盤整備 — 2026-05-17

関連 Phase: Phase 1-A（親 Phase 1）

## 実装スコープ

Phase 1 のうち、**スコープ A（`packages/core/` の方法論ドキュメント + テンプレ + JSON Schema）** のみを対象とする。
スコープ B（10 skill の `{{}}` 置換 / agent 汎用化 / hook 設定化 / `packages/shared-rendering/`）および C（下流プロジェクト固有値の設定化マッピング検証）は別 issue / 別 PR に分離する。

### 対象 deliverables

| #   | パス                                              | 種別          | 出典                                                                       |
| --- | ------------------------------------------------- | ------------- | -------------------------------------------------------------------------- |
| 1   | `packages/core/docs/stdd-methodology.md`          | 新規          | `.claude/docs/spec-driven-development-guide.md` を抽象化・汎用化           |
| 2   | `packages/core/docs/workflow-diagram.md`          | 新規          | `.claude/docs/spec-driven-development-guide.md` のフロー部分を Mermaid 化  |
| 4   | `packages/core/templates/REQUIREMENTS.md`         | 新規（移植）  | `.claude/skills/documenting-specifications/templates/requirements.md` 整理 |
| 5   | `packages/core/templates/TECH_DESIGN.md`          | 新規（移植）  | `.claude/skills/documenting-specifications/templates/tech-design.md` 整理  |
| 6   | `packages/core/templates/PLAN.md`                 | 新規（移植）  | `.claude/skills/documenting-plans/templates/plan.md` 整理                  |
| 7   | `packages/core/schema/.stdd.config.schema.json`   | 新規          | 監査レポート §3 / プラグイン分離方針 §6 から合成                           |
| 8   | `packages/core/README.md`                         | 新規          | `packages/core/` 全体の入口・利用法                                        |

### スコープ外（本 PR では対応しない）

- スコープ B: `packages/claude-code/` 配下の skill / agent の `{{}}` 置換、`packages/shared-rendering/` 実装、hook の設定化
- スコープ C: 下流プロジェクトを使った設定マッピングの実機検証
- サンプルプロジェクト（`examples/`）の用意
- npm パッケージとしての `packages/core/` の `package.json` 整備（リリース時に別 issue で対応）
- CI への `validate:schema` 組込み
- **テストファイル配置の schema 宣言** (`tests.layout` の追加): 現状テンプレートは `e2e/tests/{{app.id}}/...` 等を直接記述しており、Vitest browser mode / Cypress / Selenium / pytest-playwright 等の代替スタックでは表現できない。Phase 1-B 着手時に「`tests.layout` を schema に追加するか / プラグインに委ねるか」を決定する

---

## タスクリスト

### 1. 基盤ディレクトリ作成

- [ ] `packages/core/{docs,templates,schema}/` ディレクトリ作成

### 2. テンプレート整理（移植・汎用化）

- [ ] `packages/core/templates/REQUIREMENTS.md` 作成
  - `.claude/skills/documenting-specifications/templates/requirements.md` のテンプレート構造部分を抽出
  - 配置例の `docs/<app>/<path>/REQUIREMENTS.md` → `{{docs.layout.requirements}}` 等の Handlebars 変数化方針を併記
  - 下流プロジェクト固有値（`admin_app` / `user_app` 等の具体的サンプル名）が含まれていないことを grep で確認
- [ ] `packages/core/templates/TECH_DESIGN.md` 作成（同上）
- [ ] `packages/core/templates/PLAN.md` 作成（同上）

### 3. 方法論ドキュメント執筆

- [ ] `packages/core/docs/stdd-methodology.md` 作成
  - STDD とは何か / なぜ TDD でなく STDD か / Spec (REQUIREMENTS + TECH_DESIGN) と PLAN の役割
  - Red-Green-Refactor サイクルの位置づけ
  - 既存の `spec-driven-development-guide.md` から下流プロジェクト固有値を除去して整理
- [ ] `packages/core/docs/workflow-diagram.md` 作成
  - 新機能開発フロー（Mermaid sequence + flow）
  - 既存機能修正フロー
  - PLAN を使ったセッション分割の図示

### 4. JSON Schema 定義

- [ ] `packages/core/schema/.stdd.config.schema.json` 作成
  - `$schema`: `https://json-schema.org/draft/2020-12/schema`
  - 必須フィールド: `project`, `apps`, `commands`, `docs`
  - 任意フィールド: `workflow`, `plugins`
  - 各フィールドに `description` を付与（IDE 補完で読まれる前提）
  - 監査レポート §3 / プラグイン分離方針 §6 のマッピング表に対応する property を全て収容

### 5. `packages/core/README.md` 作成

- [ ] パッケージの目的、ディレクトリ構成、利用法（`.stdd.config.yml` をどこに置くか、テンプレをどう使うか）を記載

---

## QA（Phase 3 で実施）

- [ ] **JSON Schema 構文妥当性**: `npx -y ajv-cli compile -s packages/core/schema/.stdd.config.schema.json` がエラーなく完了
- [ ] **最小構成サンプル検証**: `apps` / `commands` 最小限の YAML を inline で書き、ajv で `validate` がパス
- [ ] **下流プロジェクト類似構成検証**: `user_app` / `admin_app` / `develop` 等を含む YAML を書き、ajv で `validate` がパス
- [ ] **禁止語 grep**: `grep -rIn -E '(admin_email|agent_staff_role|bg-\[#1e3a5f\])' packages/core/` のヒット件数 0
- [ ] **`user_app` / `admin_app` grep**: テンプレ・docs 内の `user_app|admin_app` は「例として `apps[].path` に `user_app` のような値を入れる」という参照形式のみで、ハードコード前提でないことを目視確認
- [ ] **`develop` grep**: 同上（`primary_branch` の例示としてのみ登場）
- [ ] **内部参照の整合**: ドキュメント間のリンク（`docs/<file>.md` → `templates/<file>.md` 等）が壊れていないか目視

---

## 完了基準（Phase 1-A の対応部分）

- [x] スコープ A の方法論ドキュメント 3 本が完成し、第三者が読んで「STDD フローを自プロジェクトで模倣可能」と評価できる
- [x] スコープ A のテンプレ 3 本がコピーすぐ使える状態
- [x] `packages/core/schema/.stdd.config.schema.json` が完成し、JSON Schema 検証がパス
- ⬜ MVP 対象 10 skill の Handlebars 変数化（スコープ B、別 PR）
- ⬜ `packages/shared-rendering/` レンダラ実装（スコープ B、別 PR）
- ⬜ サンプルプロジェクトでの動作確認（スコープ B 完了後、別 PR）

---

## 備考

- 本 PR は OSS 公開を見据えた基盤整備であり、下流プロジェクトの開発には直接影響しない
- Phase 0 監査時の方針に従い、下流プロジェクト固有名詞・パス・カラーコード・テストユーザー情報は一切持ち込まない
- ライセンスは Apache-2.0（Phase 0 で決定済み）
