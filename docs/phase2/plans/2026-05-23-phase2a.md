# PLAN: Phase 2-A — プラグイン分離・公開ドキュメント整備・固有名詞除去 — 2026-05-23

関連 issue: #1293（親 issue）の Phase 2-A スライス

## 実装スコープ

Phase 2-A のうち、(a) `plugins/` 配下への 5 skill の物理分離、(b) 公開リポジトリの入口ドキュメント新規作成、(c) 旧プロジェクト名・社内同期方針の最終除去、(d) `.stdd.config.yml` の `plugins` フィールド形式の JSON Schema 受入確認、までを対象とする。

仕様の正本は以下を参照:

- `docs/phase2/specs/REQUIREMENTS.md`
- `docs/phase2/specs/TECH_DESIGN.md`（特に §9 対象ファイル詳細一覧）

---

## タスクリスト

### 1. ディレクトリ・基盤準備

- [x] `plugins/nextjs-supabase/skills/` ディレクトリを `mkdir -p` で作成
- [x] `plugins/playwright/skills/` ディレクトリを `mkdir -p` で作成
- [x] `plugins/worktree/skills/` ディレクトリを `mkdir -p` で作成
- [x] `.github/ISSUE_TEMPLATE/` ディレクトリを `mkdir -p` で作成
- [x] `docs/phase2/plans/` ディレクトリ（本 PLAN の置き場）を作成

### 2. プラグイン分離（`git mv` で 5 skill を移動）

- [x] `git mv .claude/skills/implementing-ui plugins/nextjs-supabase/skills/implementing-ui`
- [x] `git mv .claude/skills/migrating-supabase plugins/nextjs-supabase/skills/migrating-supabase`
- [x] `git mv .claude/skills/e2e-testing plugins/playwright/skills/e2e-testing`
- [x] `git mv .claude/skills/run-e2e plugins/playwright/skills/run-e2e`
- [x] `git mv .claude/skills/remove-worktree plugins/worktree/skills/remove-worktree`
- [x] 上記 5 件の移動は内容書き換えと別コミットに分離する（rename 検出を確実に効かせる）※注: 作業ツリー上では移動と編集の両方が反映されているが、`git status` は 5 件すべてを `RM` (rename + modify) として検出している。commit を行う側で `git add` を 2 段階に分けて移動コミットと編集コミットに分離することを推奨
- [x] `plugins/nextjs-supabase/plugin.json` を作成（`id: "nextjs-supabase"`, `version: "0.1.0"`, `skills: ["implementing-ui", "migrating-supabase"]`）
- [x] `plugins/playwright/plugin.json` を作成（`id: "playwright"`, `version: "0.1.0"`, `skills: ["e2e-testing", "run-e2e"]`）
- [x] `plugins/worktree/plugin.json` を作成（`id: "worktree"`, `version: "0.1.0"`, `skills: ["remove-worktree"]`）

### 3. 公開ドキュメント作成（日本語）

- [x] `README.md`: 概要・対応 AI エージェント（Claude Code）・Quick Start（CLI 未実装期はテンプレート手動コピー）・ディレクトリ構成・関連ドキュメントリンク・ライセンスを 250 行以内で記述
- [x] `AGENTS.md`: agents.md 標準準拠で、プロジェクト概要 / セットアップ手順 / ビルド・テスト・lint コマンド / コードスタイル / テスト方針 / セキュリティ留意事項 / PR ガイドラインの 7 セクションを `##` 見出しで記述（TECH_DESIGN.md §3 AGENTS.md 生成ガイダンス参照）
- [x] `CONTRIBUTING.md`: コントリビューションフロー・DCO（`Signed-off-by:`）案内・PR テンプレートへの誘導を記述
- [x] `CODE_OF_CONDUCT.md`: Contributor Covenant 2.1 日本語版（`https://www.contributor-covenant.org/ja/version/2/1/code_of_conduct/`）を貼り付け、末尾に出典 URL を明記

### 4. Issue / PR テンプレート作成（日本語、各 80 行以内）

- [x] `.github/ISSUE_TEMPLATE/skill-request.md`
- [x] `.github/ISSUE_TEMPLATE/bug-report.md`
- [x] `.github/ISSUE_TEMPLATE/plugin-proposal.md`
- [x] `.github/ISSUE_TEMPLATE/agent-support-request.md`
- [x] `.github/pull_request_template.md`: 変更概要 / 関連 issue / テスト結果 / 評価結果 (eval-result, 強く推奨) / チェックリスト（DCO sign-off / docs 更新 / 破壊的変更）を含める

### 5. CareerChain 固有名詞除去（TECH_DESIGN.md §9 編集対象表に対応）

- [x] `SYNC_POLICY.md` を `git rm` で削除
- [x] `audit/PHASE0_AUDIT_REPORT.md` を `git rm` で削除し、空になった `audit/` を `rmdir` で削除
- [x] `.claude/agents/spec-writer.md`: `CareerChain（キャリアチェーン）プラットフォーム` を `対象プロジェクト` に置換
- [x] `.claude/agents/plan-writer.md`: 同上の置換
- [x] `.claude/agents/implementer.md`: 同上の置換
- [x] `.claude/agents/code-reviewer.md`: 同上の置換
- [x] `.claude/agents/qa-engineer.md`: `/home/user/careerchain/...` を汎用パスまたは `{{apps[].path}}` 参照に書き換え
- [x] `.claude/skills/software-architecture/SKILL.md`: `CareerChainプロジェクトの` を `対象プロジェクトの` に置換
- [x] `.claude/skills/auto-implement/SKILL.md`: `careerchain-worktree-<instance-id>` を `worktree-<instance-id>` に置換
- [x] `plugins/playwright/skills/run-e2e/SKILL.md`: 同様に `careerchain-worktree-` プレフィックスを `worktree-` に置換（移動後のパスで実施）
- [x] `plugins/worktree/skills/remove-worktree/SKILL.md`: 同様に `careerchain-worktree-` プレフィックスを `worktree-` に置換（移動後のパスで実施）
- [x] `plugins/nextjs-supabase/skills/implementing-ui/SKILL.md`: 残存する CareerChain 表記を `対象プロジェクト` に書き換え（移動後のパスで実施）
- [x] `plugins/nextjs-supabase/skills/migrating-supabase/SKILL.md`: 同上（移動後のパスで実施）
- [x] `docs/plugin-separation-policy.md`: §3.1 内の `CareerChain 固有の admin 権限モデル` 等の例示を一般化
- [x] `docs/phase1/plans/2026-05-17-core.md`: 社内 issue 番号 URL を削除し、`CareerChain 本体` を `下流プロジェクト` 等に置換
- [x] core 10 skill 内に `.claude/skills/{implementing-ui,migrating-supabase,e2e-testing,run-e2e,remove-worktree}` への直接パス参照が残っていないか確認し、残存していれば汎用表現に書き換え（core skill には残存なし。agents 配下の参照テーブルおよび `reverse-engineering-stdd/SKILL.md` の相対リンクをプラグイン側のパスに更新済み）

### 6. JSON Schema 確認（`plugins` フィールド形式 A / B 受入）

- [x] `packages/core/schema/.stdd.config.schema.json` の `plugins` 定義が形式 A（文字列）と形式 B（`{ id, options }`）の両方を `oneOf` で受け入れているかを確認（既存定義で対応済み）
- [x] 形式 B で `id` を必須・`options` を任意としていることを確認（`required: ["id"]` / `options` は任意プロパティ）
- [x] 不足・不備があれば schema を追補（`$id` の URL は維持する）※追補不要だった
- [x] `packages/core/README.md` に「URL 上の組織名は公開リポジトリの所在地であり、stdd の中立性に影響しない」旨の注記を追加（TECH_DESIGN.md 判断 3 / §9 スキーマ・公開 URL 表）

### 7. QA（PR description に検証コマンドを列挙して手動実行）

- [x] **プラグインディレクトリ存在確認**: `ls plugins/nextjs-supabase/skills plugins/playwright/skills plugins/worktree/skills` で 5 skill ディレクトリが正しい場所に存在
- [ ] **git 履歴追跡**: 下記 3 件すべてで移動前コミットが連続して表示される（**未コミット状態のため検証は commit 後に実施が必要**）
  - `git log --follow --oneline plugins/nextjs-supabase/skills/implementing-ui/SKILL.md`
  - `git log --follow --oneline plugins/playwright/skills/e2e-testing/SKILL.md`
  - `git log --follow --oneline plugins/worktree/skills/remove-worktree/SKILL.md`
- [x] **plugin.json 構文検証**: `for f in plugins/*/plugin.json; do echo "--- $f ---"; python3 -m json.tool "$f" > /dev/null && echo OK; done` で 3 件すべて OK
- [x] **禁止語 grep**: ヒットは `packages/core/README.md` と `packages/core/schema/.stdd.config.schema.json` の公開 URL、および `README.md` / `AGENTS.md` の git clone URL（必須）、`docs/phase2/specs/` および `docs/phase2/plans/` の本 PR 自身の対象を記述する spec/plan ドキュメント（許容）のみ
- [x] **JSON Schema 自己検証**: `npx -y ajv-cli compile -s packages/core/schema/.stdd.config.schema.json --spec=draft2020` がエラーなく完了（`--spec=draft2020` フラグが必要）
- [x] **plugins 形式サンプル検証**: TECH_DESIGN.md §4 コマンド 6 の `/tmp/sample-plugins.json`（形式 A + 形式 B 両方を含む）を `npx -y ajv-cli validate -s packages/core/schema/.stdd.config.schema.json -d /tmp/sample-plugins.json --spec=draft2020` で `valid` が出力される
- [x] **公開ドキュメント存在確認**: `ls README.md AGENTS.md CONTRIBUTING.md CODE_OF_CONDUCT.md .github/pull_request_template.md .github/ISSUE_TEMPLATE/{skill-request,bug-report,plugin-proposal,agent-support-request}.md` で 9 件すべて存在
- [x] **削除確認**: `test ! -e SYNC_POLICY.md && test ! -e audit` で両方が存在しない

---

## 完了基準（Phase 2-A スコープの全成功基準）

- [x] `.claude/skills/` 配下に core 10 skill のみが残り、移動対象 5 skill が `plugins/<plugin-id>/skills/` 配下に再配置されている（スコープ外の `security-scan` / `skill-creator` は本 PR の対象外として現状維持）
- [x] 3 つの `plugin.json` が JSON として valid
- [x] リポジトリルートに README.md / AGENTS.md / CONTRIBUTING.md / CODE_OF_CONDUCT.md が日本語で存在
- [x] `.github/ISSUE_TEMPLATE/` に 4 ファイル、`.github/pull_request_template.md` が存在（評価結果セクション含む）
- [x] `SYNC_POLICY.md` および `audit/` が削除されている
- [x] 禁止語 grep のヒットが LICENSE / NOTICE / `packages/core/` 内の公開 URL のみ（加えて Quick Start の git clone URL と本 PR の spec/plan ドキュメント自体の参照が許容例外として残るが、これらは TECH_DESIGN.md §9 の意図と一致）
- [x] `plugins` フィールドの形式 A / 形式 B の両方が ajv 検証を通過
- [x] core skill 内に移動した 5 skill への直接パス参照が 0 件
- [ ] `git log --follow` で移動後 skill の過去履歴が連続して追える（**commit 後に実施**）

---

## スコープ外（Phase 2-B / 2-C 以降に委ねる）

- **CLI `create-stdd-project` の実装**: Phase 2-B で実施
- **`examples/nextjs-supabase-todo/` 等のサンプルアプリ実装**: Phase 2-C で実施
- **v0.1.0 タグ付け・リポジトリの公開化（public flip）**: Phase 2-F で実施（ユーザー操作が必要）
- **auto-implement quick success rate ≥80% N=5 の達成**: Phase 2-C の QA gate で実施
- **`SECURITY.md` の追加**: 脆弱性報告窓口の設計とセットで Phase 2-B 〜 2-C で対応
- **プラグインの npm パッケージ化（`@stdd/plugin-*`）**: 将来の別 Phase で実施
- **プラグインの統合動作テスト（skill が実際に読み込まれるかの検証）**: Phase 2-A は物理移動とディレクトリ宣言までを対象とする
- **GitHub Actions / CI ワークフロー**: Phase 2-A ではテンプレートのみ追加し、自動化は別途
- **英訳ドキュメント**: 日本語のみで進める
- **`packages/core/templates/` の修正**: Phase 1-A で固有名詞除去済みのため対象外
